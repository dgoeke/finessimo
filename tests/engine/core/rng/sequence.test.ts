// Scaffold tests for @/engine/core/rng/sequence.ts
// import { SequenceRng } from "@/engine/core/rng/sequence";

describe("@/engine/core/rng/sequence — cycling sequence", () => {
  test.todo("Throws on empty sequence");

  test.todo("Yields the sequence in order, then wraps around");

  test.todo(
    "getNextPieces(n) returns n consecutive items with wrap-around at sequence length",
  );
});
