// Scaffold tests for @/engine/core/pieces.ts
// import { PIECES } from "@/engine/core/pieces";

describe("@/engine/core/pieces — shape sanity", () => {
  test.todo(
    "Each piece has four rotations (spawn/right/two/left) with exactly 4 cells",
  );

  test.todo(
    "O-piece rotations are identical sets; I/J/L/S/T/Z follow SRS footprint expectations",
  );

  test.todo(
    "spawnTopLeft values position spawn boxes correctly relative to SRS",
  );
});
